const ham = document.querySelector('.hamburger');
const hamburgerMenu = document.querySelector('.hamburgerMenu');

// console.log(ham);
ham.addEventListener('click', (x) => {
  // ham.classList.toggle('hamburger');
  console.log(ham);
});
